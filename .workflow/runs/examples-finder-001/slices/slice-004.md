# slice-004 — 退出码全链路、SIGINT、README 与 CI

### 1. 基本信息

- run_id: examples-finder-001
- slice_id: slice-004
- title: §8.4 全链路、Unix SIGINT 130、README、集成测试与可选 CI
- priority: P2
- owner: AI
- status: verified

### 2. 切片目标

收敛 **全局 CLI 行为**：用法错误 **2**、不可恢复错误 **3**、成功完成 **0**（含零输出）；**Unix/类 Unix** 下 SIGINT → **130**；Windows 实际码写入 README。完善 **`examples/finder/README.md`**（uv 安装、子命令示例、相对路径解析顺序、平台差异、`:` 与路径、SIGINT、编码/二进制策略与实现一致）。补充跨子命令集成测试与 fixture 补全；**若仓库已有 CI**，可选增加 `examples/finder` 测试任务（B-14）。

### 3. 输入依据

- 设计章节：§5.2、§8.3、§8.4、§10.1、§10.2、§12.2、§12.3
- 关联模块：CLI 入口（聚合、错误映射、信号处理）
- 关联接口：`--help` / 子命令 `--help`；全部退出码
- 关联数据结构：—（行为与文档）
- 前置依赖：**slice-002**、**slice-003**（均 verified）

### 4. 实现边界

#### 4.1 允许修改范围

- `examples/finder`：**任意**为达成全链路退出码、帮助、SIGINT、文档一致性所需的 CLI 与模块小改（须附带测试）
- `examples/finder/README.md`：**完整定稿**
- 仓库级 CI 配置（如 `.github/workflows/*`）：**仅当**已有 CI 且任务明确为可选扩展时添加 job/step
- 集成测试：跨 `path`/`text`、帮助、错误码

#### 4.2 禁止修改范围

- 不得引入设计外第三大主功能（§10.3）
- 不得弱化路径安全与默认不跟随 symlink（§4.2、§6）
- CI 变更不得破坏 unrelated 工作流（新增而非随意改现有 job 语义）

### 5. 预期产出

- 代码文件：CLI 顶层错误处理、信号处理（按需）
- 测试文件：集成测试、`--help` 快照或等价断言
- 配置文件：可选 CI
- 其他产物：`README.md` 定稿

### 6. 验证要求

- 设计一致性检查：§8.4 全表；`--help` 与实现一致（§10.1）
- 静态检查 / 类型检查：全包
- 单元测试：与既有测试一并通过
- 契约测试：用法错误 → 2；非法根/非法正则 → 3；无匹配 → 0；Unix SIGINT → 130（在可自动化环境中或文档+手动说明）

### 7. 偏差与升级策略

#### 7.1 允许记录偏差的条件

- Windows SIGINT 码非 130 时以 README 为准（§8.4、实现计划 §8.2）
- B-14 若仓库无 CI 或维护者暂不启用，标记为 deferred 并记入验证报告

#### 7.2 必须升级人工处理的条件

- 统一退出码需与 slice-002/003 行为冲突且无法局部修复时，须人工裁定并更新设计/风险

### 8. 完成定义

- [ ] 代码已生成或修改
- [ ] 测试已补齐
- [ ] 自动检查通过
- [ ] 偏差已记录
- [ ] 结果已归档

**Backlog**：B-11, B-12, B-13（集成与补全）, B-14（可选）。
